# NEXT

**`GOV-IMP-001` — Governance Core implementation skeleton**

See `BACKLOG.md` for the full task record. Becomes a candidate for `ready` once `AOL-001` is `done` (this PR merged) — but per `BACKLOG.md`'s `owner_decision_required` field, it additionally needs explicit Owner authorization to *implement* (not just specify) Governance Core before any session may begin it. It must not be started on `feature/autonomous-operating-layer`.
